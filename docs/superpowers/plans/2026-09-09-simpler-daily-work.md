# Simpler daily work

Approved direction: fewer simultaneous decisions, same colours and features.

1. Supplier quote sheet: compact columns for item, needed quantity, price and available quantity; labelled stacked rows on mobile. Preserve GST/inclusive tax, partial supply, cannot supply, substitutions, saved prices, commercial terms, validation, review and revisions. Keep one primary progression action and explicit totals.
2. Restaurant Today: one New purchase action and one live Needs your attention queue with request-specific links. Waiting requests remain in Purchases until they need action. Show drafts to finish, quotes ready to compare, delivery/credit follow-up and expired requests with no replies. Avoid duplicate counters/history panels; all procurement/history/reporting features remain reachable through existing navigation and contextual links.
3. Extend tenant-protected overview service to load bounded, deterministic request-specific work. Avoid fake or closed tasks and duplicate rows. Verify authorization, tenant boundaries and old delivery/credit handling.
4. Validate focused unit/integration and browser flows on full laptop and mobile widths. Keep guidance selectors and empty/loading/error states working. Reuse existing private demo only for read-only live QA; tests write local fixtures.
5. Review, PR, full release CI, authorized merge and live verification. Preserve unrelated scripts/show-tables.sh.
