# Close the procurement gaps

The user approved implementing the remaining gaps after the complete conversation audit. Continue within the existing green workspace design, English-first supplier experience, private internal demo and manual WhatsApp sharing decision. No paid integration, outbound message, invented contact, automatic award or verified-payment claim is authorized by this work.

## Independent deliverables

1. Measure first and repeated production data readiness, identify redundant requests/database work, improve the demonstrated bottlenecks, and retain active-account and tenant checks. No cross-user response cache or keep-alive cron. Report residual hosting latency honestly.
2. Let new restaurant owners self-onboard using verified Google identity without an operator email allowlist. Keep existing sign-in and invitations, explicit restaurant creation, CSRF protections, rate limits, and isolation. Password-only production owner signup remains unavailable without email verification; explain the actual signup method clearly. Remove obsolete pilot-only marketing where it contradicts the flow.
3. Let restaurant staff paste or photograph an English shopping list, review extracted names/quantities/units, and add checked rows to a purchase draft. Keep manual entry. Never infer a missing quantity/unit or overwrite existing rows silently. Reuse local OCR; no photo upload or paid model.
4. Let receiving staff read a printed invoice photo/text, review matches and billed values against their awarded items, then fill explicitly selected receiving fields. Never infer physical receipt, settle credits, or confirm a delivery automatically. Keep existing arithmetic and manual review authoritative.
5. Add user-triggered lookup of public contacts on a supplier's website using the existing safe external-fetch patterns. Bound URLs, redirects, DNS/IP destinations, time, bytes and crawl breadth; respect robots exclusions; no authentication bypass or scraping gated directories. Show source/date and review before saving; never guess an email or call the result independently verified.
6. Refresh the direct competitor comparison using current primary pages, clearly distinguish advertised features from tested products, and map QuotePlate capabilities and remaining limitations. Define a short real restaurant/supplier trial with measurable time-to-quote and completion outcomes; do not invent trial evidence.
7. Audit critical restaurant and supplier workflows, device handoffs and recovery paths. Preserve all existing features and shared layout. Document exact passing/failing coverage and platform limitations.
8. Update README, landing copy and the final 165-second 4K product overview after the interface settles. Keep optional subtitles, rounded reduced player, phone workflow and kitchen opening/closing.

## Acceptance and limits

Tests must exercise malformed inputs, ambiguous OCR, cancellation, existing-data preservation, denied access and tenant separation as applicable. Production smoke tests use the internal fictional tenant and never send messages. Real business participation, complete India-wide contact coverage, competitor uniqueness and indefinite free hosting cannot be manufactured by implementation; provide an actionable trial/cost envelope instead of claiming these are done.
